<div class="flex flex-center">
  <h1>our <%= @category.name %></h1>
</div>

<div class="grid">
  <% @category.garments.each do |garment| %>
    <div class="col-xs-6 col-sm-4">
      <%= render 'garments/card', garment: garment %>
    </div>
  <% end %>
</div>
